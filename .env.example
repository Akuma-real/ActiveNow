## ActiveNow 应用环境示例（无 Docker）
# 复制为 .env 后按需修改；运行前自行导入环境变量。

# 日志级别：error|warn|info|debug|trace
RUST_LOG=info

# 服务端口
PORT=8080

# 在线状态 TTL（秒）
PRESENCE_TTL=30

# 服务器主动 Ping 间隔（秒）；>0 开启
PING_INTERVAL=0

# 允许的 Origin 白名单（逗号分隔；空=不限制）
ALLOWED_ORIGINS=
