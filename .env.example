## ActiveNow 应用环境示例（无 Docker）
# 复制为 .env 后按需修改；运行前自行导入环境变量。

# 日志级别：error|warn|info|debug|trace
RUST_LOG=info

# 服务端口
PORT=8080

# 服务器主动 Ping 间隔（秒）；>0 开启
PING_INTERVAL=0

# 允许的来源白名单（逗号分隔；留空=不限制）
# 示例：ALLOWED_ORIGINS=https://example.com,https://sub.example.com:8443,*.corp.local
ALLOWED_ORIGINS=
